<%= content_tag 'div', nil, id: 'current-user', data: {
  user: current_user&.serialized,
  rules: current_user&.current_team&.contest&.rules&.text,
  faq: current_user&.current_team&.contest&.faq&.text,
}.to_json %>
